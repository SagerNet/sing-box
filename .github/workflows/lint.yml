# Upstream multi-arch golangci-lint — disabled on push for this Dart maintenance fork.
# Smart packages are tested inside Dart Core Release / Sync workflows.
name: Lint (upstream, disabled)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "Upstream Lint matrix disabled on the Dart fork to avoid noise."
          echo "Dart Core Release runs go test on Smart + Clash API."
